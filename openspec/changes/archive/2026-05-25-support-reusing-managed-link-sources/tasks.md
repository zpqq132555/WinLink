## 1. 已连接源聚合与候选模型
- [x] 1.1 在 ViewModel 或配套模型中定义“可复用已连接源候选”的聚合结构，按源名称、源路径、源类型去重，并为同名不同路径生成带路径的显示文案。
- [x] 1.2 调整“已连接”页的数据来源或展示映射，使相同源名称、源路径、源类型的记录在列表中聚合为一条，并合并展示其全部目标。
- [x] 1.3 为已连接源聚合与候选列表补充测试，覆盖“相同三元组聚合”和“同名不同路径不聚合”两类场景。

## 2. 新建连接页复用已连接源
- [x] 2.1 扩展 `LinkTaskDraft` 或等价任务模型，显式记录当前任务是否复用了某个已连接源。
- [x] 2.2 将“新建连接”页的备注名输入改为支持“手工输入 + 下拉选择已有源”的交互，并绑定候选列表。
- [x] 2.3 实现选择已有源后的源路径、源类型锁定行为，同时在清除选择后恢复手工编辑模式。
- [x] 2.4 补充界面相关 ViewModel 测试，覆盖“选择已有源后锁定”和“清除选择后恢复编辑”场景。

## 3. 执行合并与回归验证
- [x] 3.1 调整执行后的受管记录合并逻辑，使复用已连接源新增目标时并入已有记录，而不是创建重复源记录。
- [x] 3.2 为执行层补充测试，验证从已有源继续新增目标后，注册表中仍只有一条对应源记录且目标数量增加。
- [x] 3.3 运行 WinLink 相关测试与必要的手工界面验证，确认“已连接”聚合显示、新建连接复用已有源和批量执行回写行为都符合预期。
