## 1. 项目骨架与基础设施

- [x] 1.1 创建 WinLink 的原生桌面应用骨架、解决方案结构和主窗口入口。
- [x] 1.2 建立应用级目录约定，确保运行时可以定位 `AppData\Local\WinLink` 下的存储目录。
- [x] 1.3 定义受管链接记录、目标项、执行历史和必要 UI 状态的基础数据模型。
- [x] 1.4 定义 JSON 读写与数据版本字段，保证后续记录结构可演进。
- [x] 1.5 抽象链接操作、路径展开、状态检查和持久化访问的服务接口。

## 2. 桌面外壳与全局导航

- [x] 2.1 实现主窗口的标签页骨架：`新建链接`、`已链接`、`预设`。
- [x] 2.2 实现菜单栏入口：`退出`、`刷新已链接状态`、`执行历史`、`扫描系统已有链接`（预留）、`关于`。
- [x] 2.3 为执行历史、关于和降级确认准备辅助弹窗或对话框结构。
- [x] 2.4 确保主窗口和各标签页共享一致的布局、标题和状态反馈方式。

## 3. 新建链接工作台

- [x] 3.1 实现左侧待执行任务列表与右侧当前任务编辑区的双栏结构。
- [x] 3.2 实现任务备注名、源路径选择和自动类型锁定。
- [x] 3.3 实现目标表格及其增删改能力。
- [x] 3.4 支持“选择目标目录后自动带出默认名称”的目标添加流程。
- [x] 3.5 支持“直接指定完整目标路径”的高级目标添加流程。
- [x] 3.6 实现编辑期轻量预检查，并在目标行显示冲突或格式问题。
- [x] 3.7 实现 `先校验全部` 的完整校验流程。
- [x] 3.8 实现推荐链接策略、手动高级选项和统一降级确认对话框。
- [x] 3.9 实现尽力执行的批量创建流程，并记录每个目标项的结果。

## 4. 受管链接注册表与已链接页

- [x] 4.1 在执行成功后将受管源任务及其目标项写入 JSON 注册表。
- [x] 4.2 实现 `已链接` 页顶部总览摘要。
- [x] 4.3 实现左侧源任务列表，展示源名/备注名、目标总数和状态摘要。
- [x] 4.4 实现右侧详情区，展示源信息、最近检查时间和目标项列表。
- [x] 4.5 实现进入页面自动检查一次和手动刷新状态的流程。
- [x] 4.6 建立目标状态映射：`生效中`、`失效`、`已断开`，并提供可展开的详细原因。
- [x] 4.7 记住并恢复用户上次展开状态。

## 5. 严格删除、重建与记录管理

- [x] 5.1 实现 `删除链接` 的严格匹配检查，只允许删除仍与记录匹配的链接对象。
- [x] 5.2 为被阻止的删除操作提供明确原因和可恢复的后续动作提示。
- [x] 5.3 实现 `重建链接`，复用已记录的源和目标映射重新走创建流程。
- [x] 5.4 实现 `移除记录`，仅删除注册表项而不动当前磁盘实体。

## 6. 预设模板

- [x] 6.1 定义固定预设的数据结构，支持变量路径和说明信息。
- [x] 6.2 内置 AGENTS 主配置分发预设。
- [x] 6.3 内置 skills 目录同步预设。
- [x] 6.4 实现预设列表与右侧预览面板。
- [x] 6.5 在预览中展示展开后的真实路径、冲突状态和缺失父目录提示。
- [x] 6.6 实现预设直接应用流程，并复用标准校验/执行管道。
- [x] 6.7 实现“转成普通任务”流程，把预设实例化到 `新建链接` 工作台中继续编辑。

## 7. 执行结果与历史

- [x] 7.1 在每次执行后展示成功、跳过、失败数量摘要。
- [x] 7.2 记录最近执行历史，包括时间、任务摘要、结果数量和失败原因。
- [x] 7.3 实现执行历史弹窗，并支持查看最近一次或多次执行明细。

## 8. 验证与文档收尾

- [x] 8.1 为路径校验、目标冲突、状态判断、严格删除和降级确认补充自动化测试。
- [x] 8.2 为主流程补充必要的手工验证清单，包括新建任务、应用预设、刷新状态和重建链接。
- [x] 8.3 对照 proposal、design 和全部 spec，检查实现任务是否完整覆盖需求边界。
- [x] 8.4 在需要时补充项目内开发说明，帮助后续实现阶段快速接入。
